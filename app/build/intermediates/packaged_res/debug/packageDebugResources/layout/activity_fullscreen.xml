<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!--
        Primary WebView. Hidden (INVISIBLE) when an HTML5 video enters fullscreen.
        The VideoEnabledWebChromeClient manages its visibility.
    -->
    <com.mattlabs.websigndisplay.VideoEnabledWebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:keepScreenOn="true" />

    <!--
        Container for fullscreen video. Invisible at rest; shown by
        VideoEnabledWebChromeClient when a video requests fullscreen playback.
    -->
    <FrameLayout
        android:id="@+id/video_fullscreen_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible" />

    <!--
        Invisible settings button spanning the bottom of the screen.
        Alpha is near-zero so it is not visible to sign viewers, but
        touchscreen users can tap the bottom edge to access Settings.
    -->
    <Button
        android:id="@+id/button_settings"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_gravity="bottom"
        android:alpha="0.01"
        android:background="@android:color/transparent"
        android:contentDescription="Open Settings"
        android:text="" />

</FrameLayout>
